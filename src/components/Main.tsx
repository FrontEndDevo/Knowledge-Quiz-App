import classes from "./Main.module.scss";
const Main = () => {
  return (
    <section className={classes.main}>
      <h1>React TypeScript</h1>
      <p>Starter template for React with TypeScript</p>
    </section>
  );
};

export default Main;
